from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='How can the meaning of business entities embedded in neural models be explored via POLAR embeddings.',
    author='StjepanKusenic',
    license='MIT',
)
